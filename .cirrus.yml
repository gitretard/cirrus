env:
    CIRRUS_CLONE_DEPTH: 1
    BUILD_HOSTNAME: "b"

  
task:
 name: ssh
  persistent_worker:
    labels:
      name: AX61-2
       timeout_in: 99999999999999999h
    isolation:
      container:
        image: apon77/aosp:cirrus
        cpu: 64
        memory: 128
ssh_script:

- sudo apt-get update
- sudo apt-get install tmate
- ssh-keygen
- tmate

